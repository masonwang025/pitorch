# runtime/

GPU memory allocation, QPU enable/disable, and kernel dispatch via V3D registers and VideoCore mailbox. This is the shared layer all tests and future ops build on -- `gpu_alloc()`, `gpu_free()`, `qpu_launch()`.
